import React from 'react';
import { projectI, projectII, projectIII } from '../assets';
import styles from '../style';

const Gallery = () => {
  return (
    <section id='gallery' className={`${styles.paddingX}`}>
      
    </section>
  )
}

export default Gallery
